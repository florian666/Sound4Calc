#ifndef _SOUND4CALC
#define _SOUND4CALC

char is_SH4;

void setup(void); // configure ports to output
void Pin(void); // only on function to chnag state of the pin
char getMPU(void); //get_MPU to compatibility

#endif